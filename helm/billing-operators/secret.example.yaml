# Copy this file to secret.yaml if you add operator-level credentials later.
# secret.yaml is gitignored — never commit real secrets.
#
#   cp secret.example.yaml secret.yaml
#
# This chart currently has no secrets. values.yaml holds all non-sensitive
# configuration. Helm merges secret.yaml on top of values.yaml when present
# (see helm/Makefile).
